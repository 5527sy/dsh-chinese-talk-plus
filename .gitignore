# Python virtualenv & caches
.venv/
venv-speech/
__pycache__/
*.pyc
*.egg-info/
build/
dist/
dist-*/

# Local models (big, self-provisioned) & personal voice recordings
models/
voices/我的声音/

# Local runtime config (contains machine-specific model paths)
bridge/bridge-config.json

# Internal session docs & personal one-off scripts (not for publication)
docs/HANDOFF.md
docs/DEPLOYED.md
scripts/cdp_probe.mjs
scripts/decode_ref_audio.py
scripts/setup_models.py

# Audio & log artifacts
*.wav
*.mp3
*.log

# Node build/install artifacts
node_modules/
lib/
*.tgz
.dsh-test-home/

# Recorded output (录音/回答) — project-local, never commit
vocal/

# OS noise
Thumbs.db
.DS_Store
Desktop.ini
